{
  "name": "biomedimous",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "./node_modules/.bin/typings install --global",
    "clean": "rm -f index.js ./src/**/*.js",
    "build": "./node_modules/.bin/tsc",
    "start": "npm run clean && npm run build && node index.js",
    "aws-start": "npm install && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jordan Abderrachid <jabderrachid@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.6.6",
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "typescript": "^2.0.3",
    "typings": "^1.4.0"
  }
}
